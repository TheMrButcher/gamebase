#pragma once

namespace gamebase { namespace editor {

struct PrimitiveType {
    enum Enum {
        Float,
        Double,
        Int,
        UInt,
        Int64,
        UInt64,
        Bool,
        String
    };
};

} }
